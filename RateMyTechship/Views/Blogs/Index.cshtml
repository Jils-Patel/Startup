@model IEnumerable<RateMyTechship.Models.Blog>

@{
    ViewData["Title"] = "Index";
}

@if (User.IsInRole("Admin"))
{
    <p style="margin-top: 40px;">
        <a asp-action="Create" style="padding: 10px 20px; margin-top: 40px; background-color: orange; color: black; font-size: 20px; font-family: 'Roboto', sans-serif; border-radius: 10px;">Create New</a>
    </p>
}

<div class="card" style="border-radius: 10px; background-color: #f8f9fa; color: #212529; font-size: 22px; font-family: 'Roboto', sans-serif;">
    <div class="card-body">
        <p class="card-text">
            Interested in writing about your internship experience? 
            <a href="mailto:support@ratemytechship.com">
                <button style="color: orange; text-decoration: underline; border: none; background: none;">Contact Us!</button>
            </a>
        </p>
    </div>
</div>



<table class="table">
    @* <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Content)
            </th>
            <th></th>
        </tr>
    </thead> *@
    <tbody>
        <div class="container" style="margin-top: 10px; margin-bottom: 100px;">
            <div class="row">
                <div class="col-md-6 order-md-2" style="margin-bottom: 10px;">
                    <div class="position-sticky" style="top: 10px;">
                        <div class="card text-center" style="max-width: 80%; margin: 0 auto; border-radius: 10px; padding: 20px;">
                            <div class="d-flex flex-column align-items-center">
                                <img src="image/orange.png" alt="Orange Image" style="width: 90%; height: auto; border-radius: 10px;">
                                <p class="card-text" style="font-weight: bold; font-family: 'Roboto', sans-serif; font-size: 18px; margin-top: 20px;">Explore the latest insights, experiences, and stories from fellow interns here on our blog!</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 order-md-1">
                    @foreach (var item in Model.Reverse())
                    {
                        <div class="row mb-3">
                            <div class="col-12">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn" style="border: none; outline: none; padding: 0;">
                                    <div class="card" style="width: 400px; height: 180px; border-radius: 10px; overflow: hidden;">
                                        <img src="image/@item.BackgroundImage" class="card-img" style="width: 100%; height: 180px; object-fit: cover;" alt="Background Image">
                                        <div style="font-weight: bold; font-family: 'Roboto', sans-serif; position: absolute; bottom: 0; left: 0; width: 100%; height: 100%; padding: 20px; background-color: rgba(0, 0, 0, 0.5); color: white; text-align: left; box-sizing: border-box;">
                                            <h2 class="card-title" style="font-size: 23px; margin-top: 70px; font-weight: bold;">@item.Title</h2>
                                            <h6 class="card-subtitle mb-2" style="font-size: 17px; font-weight: bold;">By @item.Author</h6>
                                            <p class="card-text" style="font-size: 14px;">@item.Date</p>
                                        </div>
                                    </div>
                                </a>
                                @if (User.IsInRole("Admin"))
                                {
                                    <div class="mt-2">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                                    </div>
                                }
                            </div>
                        </div>
                    }
                </div>
                @* <div class="col-md-6 d-flex justify-content-center align-items-center">
                    <div class="card text-center" style="width: 80%; border-radius: 10px; padding: 20px;">
                        <div class="d-flex flex-column align-items-center">
                            <img src="image/orange.png" alt="Orange Image" style="width: 90%; height: auto; border-radius: 10px;">
                            <p class="card-text" style="font-family: 'Roboto', sans-serif; font-size: 18px; margin-top: 20px;">Welcome to our Blog! We're glad to have you here! Explore the latest insights, tips, and experiences from fellow interns!</p>
                        </div>
                    </div>
                </div> *@
            </div>
        </div>

        @* <div class="row" style="margin-top: 10px; margin-bottom: 50px;">
            <div class="col-md-12">
                @foreach (var item in Model.Reverse())
                {
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn" style="border: none; outline: none;">
                        <div class="card" style="width: 400px; height: 180px; border-radius: 10px; overflow: hidden;">
                            <img src="image/@item.BackgroundImage" class="card-img" style="width: 100%; height: auto; object-fit: cover;" alt="Background Image">
                            <div style="font-weight: bold; font-family: 'Roboto', sans-serif; position: absolute; bottom: 0; left: 0; width: 100%; height: 100%; padding: 20px; background-color: rgba(0, 0, 0, 0.5); color: white; text-align: left; box-sizing: border-box;">
                                <h2 class="card-title" style="font-size: 24px; margin-top: 70px; font-weight: bold;">@item.Title</h2>
                                <h6 class="card-subtitle mb-2" style="font-size: 17px; font-weight: bold;">By @item.Author</h6>
                                <p class="card-text" style="font-size: 14px;">@item.Date</p>
                            </div>
                        </div>
                          @* <div class="card-footer">
                    <div class="btn-group">
                    @if (User.IsInRole("Admin"))
                    {
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                    }
                            </div> 
                         </div>  *@ 
                    @* </a> *@
                    @* @if (User.IsInRole("Admin"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                    } *@
                @* }
            </div> *@
           @*  <div class="card-footer">
                <div class="btn-group">
                    @* <button type="button" class="btn btn-sm btn-outline-success thumbs-up" style="padding: 10px 20px; font-size: 18px;">
                    &#x1F44D;
                    <span class="badge bg-success">0</span>
                    </button>
                    <button type="button" class="btn btn-sm btn-outline-danger thumbs-down" style="padding: 10px 20px; font-size: 18px;">
                    &#x1F44E;
                    <span class="badge bg-danger">0</span>
                    </button> *@
                    @* <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm btn-primary">Edit</a> *@
                    @*<a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm btn-secondary" style="background-color: orange; color: black;">View Details</a> *@
                    @* <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-sm btn-danger">Delete</a> *@
                @* </div> *@
            @* </div> *@
        @* </div> *@

    </tbody>
</table>